/** @type {import('next').NextConfig} */
const nextConfig = {
    // Your essential settings
    reactStrictMode: true,
    
    // Only add other options if you specifically need them Test
    // Minimal configuration is preferred for Next.js on Vercel
  };
  
  module.exports = nextConfig;